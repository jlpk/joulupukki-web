'use strict';

angular.module('joulupukki.live')

 
/*
    .service('getLatestProjects', ['$http',
        function ($http) {
            return function () {
                return $http.get('/v3/projects?limit=30&get_last_build=1')
                    .error(function () {
                        throw new Error('getLatestProjects : GET Request failed');
                    });
            };
        }]);
*/
